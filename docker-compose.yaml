# For testing prod version of the app
version: '3.8'
services:
  web:
    image: jackson147/wadebridge-gaming-group:latest
    build: .
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
    volumes:
      - .env:/app/.env
    command: npm start